<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 어노테이션 기반 컨트롤러 활성화 -->
    <mvc:annotation-driven validator="validator" />
    <mvc:resources mapping="/static/**" location="/static/" />
    <bean id="validator"
          class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean" />





    <!-- 컴포넌트 스캔 -->
    <context:component-scan base-package="org.example.ramian_pj" />

    <!-- ViewResolver 설정 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>


    <mvc:interceptors>
        <!-- 로그인 체크 -->
        <mvc:interceptor>
            <mvc:mapping path="/reserve/**"/>
            <bean class="org.example.ramian_pj.conf.LoginCheckInterceptor"/>
        </mvc:interceptor>

        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/>

            <!-- 로그인/로그아웃/첫진입을 'admin' 기준으로 제외 -->
            <mvc:exclude-mapping path="/admin"/>
            <mvc:exclude-mapping path="/admin/join"/>
            <mvc:exclude-mapping path="/admin/id_ck"/>
            <mvc:exclude-mapping path="/admin/logout"/>
            <mvc:exclude-mapping path="/admin/logout/**"/>


            <!-- (정적 리소스가 admin 하위면) -->
            <mvc:exclude-mapping path="/admin/css/**"/>
            <mvc:exclude-mapping path="/admin/js/**"/>
            <mvc:exclude-mapping path="/admin/images/**"/>

            <bean class="org.example.ramian_pj.conf.AdminLoginCheckInterceptor"/>
        </mvc:interceptor>



        <mvc:interceptor>
            <mvc:mapping path="/reserve/cancel"/>
            <mvc:mapping path="/reserve/modify"/>
            <bean class="org.example.ramian_pj.conf.ReserveCheckInterceptor">
                <constructor-arg ref="reserveService"/>
            </bean>

        </mvc:interceptor>

    </mvc:interceptors>
</beans>
